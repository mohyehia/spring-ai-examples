{
  "mcpServers": {
    "tavily-search": {
      "command": "cmd.exe",
      "args": [
        "/c",
        "npx",
        "-y",
        "tavily-mcp@0.2.0"
      ],
      "env": {
        "TAVILY_API_KEY": "${env.TAVILY_API_KEY}"
      }
    }
  }
}